#include <iostream>
#include <cmath> // Biblioteca de matematica

int main(){

	double x = 4;
	double y = 3.14;
	double z;
	//Funçoes uteis de matematica
	
	//z = std::max(x ,y); //Mostra o maior valor entre as variaveis

	//z = std::min(x, y); //Mostra o menor valor entre as variaveis

	//z = pow(5,2); //Eleva o valor A pelo valor B

	//z =sqrt(x); //Funçao que faz a raiz quadrada de um valor

	//z = abs(-10); //Funçao absoluta retorna o quao longe de 0 o valor esta

	//z = round(y); //Arredonda o numero;
	
	//z = ceil(y); //Arredonda pra cima;

	//z = floor(y); //Arredonda pra baixo;

	std::cout << z << "\n";
	return 0;
}
